[package]
name = "prompt-tool"
version = "1.0.0"
description = "Prompt Tool 提示词助手"
license = "MIT"
authors = ["baoxinwen"]
edition = "2021"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "prompt_tool_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["image-png", "tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-global-shortcut = "2.3.2"
tauri-plugin-dialog = "2.7.2"
tauri-plugin-single-instance = "2.4.3"
tauri-plugin-autostart = "2.5.1"
tauri-plugin-updater = "2"
arboard = "3.6.1"
uuid = { version = "1.26.0", features = ["v4"] }
ureq = "2"
base64 = "0.22"
image = { version = "0.25.10", features = ["png"] }
# 凭据存 OS 凭据库（Windows 凭据管理器），data.json 只存哨兵占位
keyring = { version = "3", features = ["windows-native"] }
# 按键注入/前台恢复/剪贴板序号等 Win32 调用（本项目仅支持 Windows）
windows-sys = { version = "0.61.2", features = ["Win32_Foundation", "Win32_System_DataExchange", "Win32_System_Threading", "Win32_UI_Input_KeyboardAndMouse", "Win32_UI_WindowsAndMessaging"] }

[dev-dependencies]
tempfile = "3"

