# Changelog

本项目所有显著变更将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 剪贴板清空撤销：清空后弹出通知条可一键恢复，同步侧恢复时清除墓碑并刷新 `copied_at`，防止其他设备被同步回退误删
- 云同步 `lastSyncAt`：`run_sync` 成功后统一记录最近同步时间，云同步页展示"最近同步"状态
- 剪贴板历史按时间分组（今天/昨天/更早），列表行增加类型图标
- 快捷面板与提示词列表的变量 chip 预览：`{{变量|说明}}` 在预览中以高亮 chip 呈现

### Changed

- 七页 UI 定稿实施（对照 `docs/prototypes/` 定稿原型）：
  - 设计 token 对比度达标（亮/暗两主题文字与边框对比度修正）
  - 剪贴板页精修清单：工具栏重排（搜索居右、清空独立），行样式与类型图标
  - 快捷面板变量 chip 预览，面板改为上 1/3 定位，底部操作区随变量提示变化（"填写 N 个变量"）
  - 提示词列表行重构：徽章行（标签徽章、使用次数、变量数 chip、快捷键）
  - 提示词详情：变量卡改为真输入框，正文与预览镜像高亮变量；底栏按钮层级对齐定稿原型（删除独立居左，主操作"填写变量并粘贴/复制"居右）
  - 云同步页单卡收敛：状态、操作、开关与配置合并为一张卡，未配置时引导"去填写"
  - 数据页语义明示：导入区增加"跳过重复条目"说明横幅，新增数据概览卡（条目统计与打开数据目录）
  - 设置页：快捷键行改为水平布局，清除按钮改橡皮擦图标并支持整槽录制；通用区开关右对齐
- 导出按钮取消默认主按钮样式，数据页导入/导出层级重新分配

### Fixed

- 启动图片对账误删在用图片：引用集合收集的是 `ImageRef.file`（`<id>.png` 文件名），孤儿判定却按裸 id 比对，导致每次启动都把全部在用图片当孤儿删除（自 v1.1.0 起），表现为剪贴板图片缩略图全部裂开、仅本次运行新捕获的图片可显示；判定改为按主图文件名比对，缩略图随主图保留，测试改用与生产一致的数据形状并补回归边界用例。已被删除的图片文件无法找回，对应历史条目仍会显示裂图，可手动删除条目或清空历史

## [1.1.0] - 2026-09-09

独立评审门禁（全项目逐组件/逐按钮）产出的 84 条意见处置后的首个版本。

### Fixed

- 快捷面板：中文输入法组合态按 Enter/Esc 不再误触粘贴或隐藏面板；`{{clipboard}}` 填充改用函数替换，剪贴板含 `$&`/`$$` 等序列不再被改写后粘出
- 变量系统：变量值表改用无原型对象并只认自有键，`__proto__` 等原型链变量名不再丢失填写值或输出 `[object Object]`
- 热键：录制 shift+标点组合按物理键位归一，注册不再失败；注入前等待物理修饰键释放，快速捕获与粘贴不再间歇性失败
- 捕获窗：键盘处理改为窗口级监听，失焦后 Esc/Ctrl+S 不再失效；变量窗与分类编辑框补输入法守卫
- 设置：保存改为串行化队列，快速连续变更不再互相回滚；云同步页离开守卫防止未保存配置丢失；开关恢复键盘可操作，次级文字对比度达 WCAG AA
- 云同步：关闭剪贴板同步的设备上传时保留云端既有剪贴板（不再清空云端副本）；Gist ID 自动回填不再被脏表单覆盖，避免重复创建 secret Gist；网络错误文本脱敏 URL 内嵌凭据
- 剪贴板：图片监听改用系统序号预检，纯图片内容不再每 700ms 全量读取；面板文本粘贴改为写入成功后才隐藏窗口
- 性能与健壮性：落盘类命令改为后台线程执行不再阻塞界面；变量记忆并发安全、读取失败拒绝写回；启动自动清理无引用的孤儿图片；快捷面板/捕获窗不再接收明文凭据

### Changed

- 项目定位收敛为 Windows 单平台，移除跨平台编译桩与 macOS 声明
- README 具名披露更新镜像 ghfast.top 及其隐私影响


## [1.0.0] - 2026-09-08

首个公开发布版本。

### Added

- 快捷面板：全局快捷键（默认 `Alt+Q`，可自定义）呼出，搜索 → 回车自动粘贴到当前输入框，`→` 展开完整预览
- 提示词管理：分类筛选、标题/标签/内容全文搜索、拼音首字母搜索、置顶与使用频率排序
- 变量占位符：`{{变量名|说明}}` 调用时弹窗填写后拼接成最终文本；提示词可绑定独立快捷键直接粘贴
- 快速捕获：选中文本按 `Alt+S` 一键保存为新提示词
- 剪贴板历史：自动记录复制的文本与图片，可开关、可搜索、可再粘贴，可选粘贴后恢复原剪贴板
- 云同步：WebDAV（坚果云等）与 GitHub secret Gist 两种渠道，条目级合并、删除传播、启动与变更后自动同步
- 应用内自动更新：启动后异步检查更新（可关闭），更新包经 minisign 签名校验，应用内一键安装
- 数据安全：全部数据仅存本机，保存时自动备份，损坏文件自动隔离并可从备份恢复
- 导入导出：JSON 完整备份 / Markdown / TXT 批量导入导出
- 亮色/暗色/跟随系统主题，系统托盘常驻
